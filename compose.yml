services:
  web:
    image: nginx:1.27-alpine
    container_name: mediapeople-web
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
    ports:
      - "8095:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./styles.css:/usr/share/nginx/html/styles.css:ro
      - ./app.js:/usr/share/nginx/html/app.js:ro
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro

  web-mini:
    image: nginx:1.27-alpine
    container_name: mediapeople-web-mini
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
    ports:
      - "8096:80"
    volumes:
      - ./mini.html:/usr/share/nginx/html/index.html:ro
      - ./styles.css:/usr/share/nginx/html/styles.css:ro
      - ./app.js:/usr/share/nginx/html/app.js:ro
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro

  web-matchmaker:
    image: nginx:1.27-alpine
    container_name: mediapeople-web-matchmaker
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
    ports:
      - "8097:80"
    volumes:
      - ./matchmaker.html:/usr/share/nginx/html/index.html:ro
      - ./styles.css:/usr/share/nginx/html/styles.css:ro
      - ./app.js:/usr/share/nginx/html/app.js:ro
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro

  web-admin:
    image: nginx:1.27-alpine
    container_name: mediapeople-web-admin
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
    ports:
      - "8098:80"
    volumes:
      - ./admin.html:/usr/share/nginx/html/index.html:ro
      - ./styles.css:/usr/share/nginx/html/styles.css:ro
      - ./app.js:/usr/share/nginx/html/app.js:ro
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro

  api:
    build:
      context: ./server
    container_name: mediapeople-api
    restart: unless-stopped
    environment:
      PGHOST: postgres
      PGPORT: 5432
      PGDATABASE: ${POSTGRES_DB:-mediapeople}
      PGUSER: ${POSTGRES_USER:-mediapeople}
      PGPASSWORD: ${POSTGRES_PASSWORD}
      PORT: 3000
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3000"

  postgres:
    image: postgres:16-alpine
    container_name: mediapeople-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mediapeople}
      POSTGRES_USER: ${POSTGRES_USER:-mediapeople}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
      - ./backup/postgres:/backup
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
